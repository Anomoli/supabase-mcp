# LiveKit configuration for the future approved SIP activation window.
# API credentials remain external and arrive through LIVEKIT_KEYS.
port: 7880
redis:
  address: redis:6379
rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 50060
  use_external_ip: false
logging:
  level: info
